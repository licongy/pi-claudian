---
"pi-auto-save-to-markdown": patch
---

Rename the package from `auto-save-to-markdown` to `pi-auto-save-to-markdown`, following the Pi community's `pi-` prefix convention for unscoped extension packages. Install with `pi install npm:pi-auto-save-to-markdown`; the previous unscoped name is deprecated.
